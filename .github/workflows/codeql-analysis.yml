name: CodeQL analysis

on:
  push:
    branches: [ test-ubuntu-24.04 ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 3 * * 0'

jobs:
  call-codeQL-analysis:
    name: CodeQL analysis 
    uses: aparnajyothi-y/reusable-workflows/.github/workflows/codeql-analysis.yml@main
