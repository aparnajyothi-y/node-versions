name: Create Pull Request
on:
  push:
    branches: [ test-ubuntu-24.04 ]

jobs:
  create-pr:
    uses: aparnajyothi-y/versions-package-tools/.github/workflows/create-pr-to-update-manifest.yml@main
    with:
      tool-name: "node"
    secrets: inherit
